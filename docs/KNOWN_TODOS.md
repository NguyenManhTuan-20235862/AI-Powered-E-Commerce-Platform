# Known TODOs — Technical Debt Có Chủ Đích

File này dùng để theo dõi technical debt có chủ đích — khi xử lý xong 1 mục, cập nhật cột Trạng thái thành "Đã xử lý" kèm ngày, không xóa dòng khỏi bảng (giữ lịch sử).

| # | Vấn đề | Task WBS liên quan | Ưu tiên | Trạng thái | Ghi chú |
|---|---|---|---|---|---|
| 1 | **Ownership check thiếu ở Order endpoints.** `GET /orders/{order_id}` và `PUT /orders/{order_id}/cancel` hiện chỉ check đúng role (Customer) qua `require_role()`, nhưng CHƯA check user gọi API có đúng là chủ đơn hàng đó không (so sánh `order.user_id == current_user.id`). | 3.4.2 (API giỏ hàng & đặt hàng) | Cao | Chưa xử lý | Lỗ hổng bảo mật thật nếu bỏ sót — user A có thể xem/hủy đơn của user B. TODO comment đã có sẵn trong `app/routers/order.py`. Phát sinh từ task 1.3.3. |
| 2 | **WebSocket `/ws/chat` chưa có xác thực.** `get_current_user`/`require_role` dùng `OAuth2PasswordBearer`, không áp dụng trực tiếp cho WebSocket handshake — cần cơ chế riêng (đọc token qua query string hoặc subprotocol). | 5.1.1 (Backend WebSocket endpoint) hoặc 6.1.x (AI Agent) | Cao | Chưa xử lý | Nếu bỏ sót: bất kỳ ai cũng gọi được AI Agent, tốn phí OpenAI API vô tội vạ — liên quan trực tiếp tới rate limiting (task 8.3). Phát sinh từ task 1.3.3. |
| 3 | **SSE middleware — `duration_ms` sai lệch + overhead `BaseHTTPMiddleware`.** Middleware log request hiện dùng `BaseHTTPMiddleware`, đo `duration_ms` không phản ánh đúng thời gian giữ kết nối thật của SSE (log gần như ngay khi header response bắt đầu, không đợi tới khi client đóng kết nối). `BaseHTTPMiddleware` có overhead nhỏ (chạy downstream trong task riêng) — cân nhắc viết lại bằng raw ASGI middleware nếu có nhiều SSE client đồng thời. | 5.2.1 (Backend SSE endpoint trạng thái đơn hàng) | Trung bình | Chưa xử lý | Chỉ ảnh hưởng khi SSE có logic thật + nhiều kết nối đồng thời. Phát sinh từ task 1.4.2. |
| 4 | **`requirements-ai.txt` (langchain, langchain-openai) chưa được cài trong `Dockerfile.dev` lẫn `Dockerfile.prod`.** Đã tách khỏi `requirements-core.txt` để image nhẹ hơn khi chưa có code AI Agent nào dùng tới (xác nhận bằng grep `app/` + `tests/`, không có import nào). Cả 2 Dockerfile đã có sẵn khối comment đánh dấu chỗ cần bỏ comment để thêm layer cài file này. | 6.1 (AI Agent - bắt đầu code) | Trung bình | Chưa xử lý | Khi bắt đầu code AI Agent, cần sửa **cả 2** Dockerfile: <br>**`backend/Dockerfile.dev`**: bỏ comment layer `requirements-ai.txt` đã đánh dấu sẵn sau layer `requirements-core.txt`. <br>**`backend/Dockerfile.prod`**: trong stage `builder`, thêm layer mới cài `requirements-ai.txt` **SAU** layer `requirements-core.txt` + `requirements-prod.txt` hiện có — giữ TÁCH layer riêng (không gộp chung 1 `RUN pip install`) để đúng nguyên tắc cache đã dùng xuyên suốt (đổi AI deps không làm invalidate cache của layer core/prod, và ngược lại):<br>`COPY requirements-ai.txt .`<br>`RUN pip install --no-cache-dir -r requirements-ai.txt`<br>Không cần Dockerfile.prod riêng biệt/khác nhánh cho AI - kiến trúc multi-stage + venv hiện tại đã sẵn sàng mở rộng, chỉ là thêm 1 layer. Sau khi bật layer AI, cân nhắc merge `requirements-core.txt`+`requirements-ai.txt` lại thành 1 file nếu không còn lý do tách. Phát sinh từ task 2.1.1, cập nhật thêm ở task 2.1.2 (sau khi Dockerfile.prod tồn tại). |
| 5 | **Frontend Docker image gắn chặt `NEXT_PUBLIC_API_URL` lúc build time — không thể "build once, promote" như Backend.** Đã tự kiểm chứng ở task 2.2.2: giá trị truyền qua `--build-arg` lúc `docker build` bị Next.js nhúng cứng vào file JS tĩnh (`.next/static/`) trong lúc `next build`; đổi biến này lúc `docker run -e ...` không có tác dụng gì. Backend không gặp vấn đề này (đọc `.env`/biến môi trường lúc runtime qua `pydantic-settings`, cùng 1 image dùng được cho nhiều environment). | 7.5.2 (Deploy Frontend) hoặc 8.4 (nếu có CI/CD pipeline) | Trung bình | Chưa xử lý | Cần quyết định TRƯỚC khi deploy nhiều environment: <br>**(a)** Build riêng 1 image cho mỗi environment trong CI (mỗi lần deploy staging/production đều `docker build --build-arg NEXT_PUBLIC_API_URL=<url-riêng-env-đó>` — không tái sử dụng chung 1 image giữa các environment). <br>**(b)** Route qua reverse-proxy same-origin (`/api/*` trỏ ngược vào Backend, phía server) — `NEXT_PUBLIC_API_URL` cố định là path tương đối, không đổi theo environment, tránh phải rebuild khi đổi environment, nhưng cần thêm cấu hình proxy (Nginx/Next.js rewrites...) chưa có trong repo. <br>Ưu tiên Trung bình vì hiện dự án solo, nhiều khả năng chỉ có 1 môi trường production duy nhất (không có staging riêng) — cân nhắc lại mức độ cần thiết của (a)/(b) khi thực sự tới gần task 7.5.2, có thể không cần xử lý gì nếu chỉ deploy 1 environment. Phát sinh từ task 2.2.2. |
| 6 | **`backend/.env.example` có `DATABASE_URL` trỏ database tên `ecommerce_db` — KHÔNG khớp** với database thật `ecommerce` đang dùng ở container MySQL thật (trước là `mysql-dev`, giờ là service `mysql` trong `docker-compose.yml`, task 2.3.1). Phát hiện khi tạo `.env.example` gốc repo cho compose — phải dùng đúng tên `ecommerce` để khớp dữ liệu đã migrate, làm lộ ra `backend/.env.example` chưa từng được đồng bộ theo tên DB thật. | 2.3.4 (đưa Backend vào compose) hoặc bất kỳ lúc nào set up `backend/.env` mới | Thấp | **Đã xử lý** (2026-08-05) | Đổi placeholder trong `backend/.env.example` thành `ecommerce`, kèm comment nhắc đồng bộ với `MYSQL_DATABASE` trong `docker-compose.yml` nếu 1 trong 2 đổi sau này. |
| 7 | **`backend/.env.example` có `MONGO_URI=mongodb://localhost:27017` — KHÔNG có username/password** — không còn dùng được nguyên văn với service `mongodb` thật trong `docker-compose.yml` (task 2.3.2), vì service này đã bật auth qua `MONGO_INITDB_ROOT_USERNAME`/`MONGO_INITDB_ROOT_PASSWORD` (đã tự kiểm chứng: ghi dữ liệu không có credential bị MongoDB từ chối với lỗi "Command insert requires authentication"). | 2.3.4 (đưa Backend vào compose) hoặc bất kỳ lúc nào set up `backend/.env` mới để test với MongoDB thật qua compose | Thấp | **Đã xử lý** (2026-08-05) | Đổi thành `mongodb://root:<password>@localhost:27017/?authSource=admin` (placeholder `<password>`, không phải giá trị thật) kèm comment giải thích `authSource=admin` bắt buộc. Khi Backend vào compose (task 2.3.4), đổi host `localhost` → `mongodb` (tên service). |
| 8 | **`backend/.env.example` có `REDIS_URL=redis://localhost:6379/0` — KHÔNG có password** — không còn dùng được nguyên văn với service `redis` thật trong `docker-compose.yml` (task 2.3.3), vì service này đã bật `--requirepass` (đã tự kiểm chứng: `SET`/`GET` không có `-a` bị Redis từ chối với lỗi "NOAUTH Authentication required"). Cùng loại vấn đề với TODO #6 (MySQL) và #7 (MongoDB) - lần này là Redis. | 2.3.4 (đưa Backend vào compose) hoặc bất kỳ lúc nào set up `backend/.env` mới để test với Redis thật qua compose | Thấp | **Đã xử lý** (2026-08-05) | Đổi thành `redis://:<password>@localhost:6379/0` (placeholder `<password>`, không phải giá trị thật) kèm comment giải thích cú pháp không có username (khác MySQL/MongoDB). Khi Backend vào compose (task 2.3.4), đổi host `localhost` → `redis` (tên service). |
| 9 | **`RegisterForm.tsx`/`LoginForm.tsx` giả định response có field `data.data.user` — Backend KHÔNG BAO GIỜ trả field này.** Phát hiện lúc verify luồng thật task 2.3.4 (đăng ký → đăng nhập qua toàn bộ chuỗi container): `POST /auth/register` trả `201` với `UserResponse` thật (đúng `docs/API_SPEC.md`, KHÔNG có token) nhưng `RegisterForm.tsx` cố đọc `data.data.access_token`/`user.role` → `undefined.role` throw lỗi, catch bởi generic error handler → UI hiện "Tạo tài khoản thất bại" dù tài khoản ĐÃ được tạo thật trong DB. Tương tự, `POST /auth/login` trả `200` với `TokenPair` đúng spec (`access_token`, `refresh_token`, `token_type` - KHÔNG có `user`) nhưng `LoginForm.tsx` đọc `data.data.user.role` → cùng lỗi → UI hiện "Đăng nhập thất bại" dù `setTokens()` đã chạy THÀNH CÔNG trước đó (đã tự kiểm chứng: `sessionStorage` có `ecommerce_access_token`/`ecommerce_refresh_token` hợp lệ ngay sau khi bấm nút, dù UI báo lỗi). | Không thuộc WBS Docker (2.1-2.3) - lỗi logic Frontend thuộc phạm vi task 1.3.2/1.4.x (Auth UI) đã code trước đó | Cao | **Đã xử lý** (2026-08-05) | `RegisterForm.tsx`: register thành công (201) → redirect `/login?registered=1`, không tự đăng nhập bằng token không tồn tại - `LoginForm.tsx` đọc query param này (`useSearchParams`, cần bọc `<Suspense>` trong `login/page.tsx` - Next.js App Router bắt buộc) để hiện banner "Đăng ký thành công, vui lòng đăng nhập.". `LoginForm.tsx`: login thành công → gọi thêm `GET /auth/me` (interceptor tự gắn token vừa lưu) lấy role thật → redirect `/admin/dashboard` (customer: `/`). **Backend đổi kèm**: `GET /auth/me` (`app/routers/auth.py`) trước đó CŨNG vẫn là `501` (gap riêng, phát hiện lúc sửa fix này) - đã implement thật, y hệt logic `GET /users/me` đã có (`success_response(data=UserResponse.model_validate(current_user))`). Verify thật qua trình duyệt: đăng ký → banner đúng → login customer → redirect `/` → login admin (`admin@example.com`) → redirect `/admin/dashboard`, không còn lỗi `undefined.role`, `sessionStorage` có token hợp lệ đúng lúc, `pytest -q` 42/42 pass (sau khi tạo thêm DB `ecommerce_test` còn thiếu trong container MySQL mới - gap khác, không liên quan trực tiếp fix này). Redirect admin dùng `/admin/dashboard` thay vì `/admin` (không có `page.tsx` gốc, sẽ 404) - phát hiện thêm lúc verify. |
| 10 | **CORS `allow_origins=["*"]` đang hard-code** (`backend/app/main.py`, `CORSMiddleware`) — chấp nhận request từ BẤT KỲ origin nào, kèm `allow_credentials=True`. Code đã có sẵn `# TODO: giới hạn allow_origins về domain thật của frontend trước khi lên production.` ngay tại chỗ. Phát hiện lại lúc rà soát toàn diện task 2.4.2 (`docs/ENV_VARIABLES.md`, mục "Chưa có trong `.env` nhưng đang hard-code"). | 7.5 (Deploy) hoặc xử lý sớm hơn nếu muốn | Trung bình | Chưa xử lý | Không chặn dev (wildcard tiện test, mọi origin gọi được). BẮT BUỘC xử lý trước khi deploy thật - wildcard CORS kết hợp `allow_credentials=True` là lỗ hổng bảo mật (website bất kỳ có thể gọi API kèm cookie/credential của user đã đăng nhập). Sửa: thêm biến `CORS_ALLOWED_ORIGINS` (danh sách domain, phân tách dấu phẩy) đọc qua `app/core/config.py`, dev để `*` hoặc `http://localhost:3000`, production set đúng domain Frontend thật. |
