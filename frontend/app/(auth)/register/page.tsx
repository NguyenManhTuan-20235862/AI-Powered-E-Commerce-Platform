"use client";

// TODO (Thành viên B): form đăng ký, gọi POST /auth/register qua lib/axios.
export default function RegisterPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-brand-800">Đăng ký</h1>
      <p className="mt-2 text-sm text-brand-500">Form đăng ký - sẽ triển khai sau.</p>
    </div>
  );
}
