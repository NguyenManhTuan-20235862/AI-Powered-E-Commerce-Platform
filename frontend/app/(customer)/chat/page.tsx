"use client";

// TODO (Thành viên B): kết nối WebSocket tới backend /api/v1/chat/ws/{session_id}.
export default function ChatPage() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-10">
      <h1 className="font-heading text-xl text-foreground">Chat với AI</h1>
      <p className="mt-2 text-sm text-foreground-muted">Chat AI (WebSocket) - sẽ triển khai sau.</p>
    </div>
  );
}
