"use client";

// TODO (Thành viên B): form checkout (CSR), tạo đơn hàng qua lib/axios.
export default function CheckoutPage() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-10">
      <h1 className="text-xl font-semibold text-brand-800">Thanh toán</h1>
      <p className="mt-2 text-sm text-brand-500">Checkout (CSR) - sẽ triển khai sau.</p>
    </div>
  );
}
