// TODO (Thành viên B): fetch danh sách sản phẩm phía server (SSR) từ backend qua lib/axios.
export default function ProductsPage() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-10">
      <h1 className="text-xl font-semibold text-brand-800">Danh sách sản phẩm</h1>
      <p className="mt-2 text-sm text-brand-500">Catalog sản phẩm (SSR) - sẽ triển khai sau.</p>
    </div>
  );
}
