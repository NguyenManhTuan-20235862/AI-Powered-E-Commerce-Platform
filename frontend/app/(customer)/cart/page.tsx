"use client";

// TODO (Thành viên B): quản lý giỏ hàng phía client (CSR), gọi API qua lib/axios.
export default function CartPage() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-10">
      <h1 className="text-xl font-semibold text-brand-800">Giỏ hàng</h1>
      <p className="mt-2 text-sm text-brand-500">Giỏ hàng (CSR) - sẽ triển khai sau.</p>
    </div>
  );
}
