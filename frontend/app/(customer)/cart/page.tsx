"use client";

// TODO (Thành viên B): quản lý giỏ hàng phía client (CSR), gọi API qua lib/axios.
export default function CartPage() {
  return (
    <div className="mx-auto max-w-5xl px-4 py-10">
      <h1 className="font-heading text-xl text-foreground">Giỏ hàng</h1>
      <p className="mt-2 text-sm text-foreground-muted">Giỏ hàng (CSR) - sẽ triển khai sau.</p>
    </div>
  );
}
