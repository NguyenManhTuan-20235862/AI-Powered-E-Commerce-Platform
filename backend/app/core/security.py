"""Placeholder cho logic bảo mật: hash mật khẩu (passlib) và JWT (python-jose).

TODO (Thành viên A - module Auth, tuần sau):
- hash_password / verify_password dùng passlib.CryptContext
- create_access_token / decode_access_token dùng jose.jwt + settings.JWT_SECRET_KEY
- get_current_user dependency (OAuth2PasswordBearer) để bảo vệ các route cần login
"""
